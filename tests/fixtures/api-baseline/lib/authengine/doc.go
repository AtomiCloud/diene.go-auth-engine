// Package authengine is the published v1.0.0 baseline of the AtomiCloud Go
// auth engine. This fixture is the API-compatibility reference the candidate
// release is compared against; it deliberately carries only the
// dependency-free half of the surface so the baseline module builds with no
// requires of its own.
package authengine
